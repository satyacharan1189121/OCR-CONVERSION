[project]
name = "ocr-conversion"
version = "0.1.0"
requires-python = "~=3.12.0"
dependencies = [
  "Flask==3.1.2",
  "flask-cors==6.0.2",
  "google-genai==1.56.0",
  "pillow==12.1.0",
  "python-dotenv==1.2.1",
]

[tool.vercel]
entrypoint = "server:app"

